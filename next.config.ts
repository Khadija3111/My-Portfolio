import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  // Base path for GitHub Pages – replace with your repo name if it differs
  basePath: "/My-Portfolio",
  assetPrefix: "/My-Portfolio/",
};

export default nextConfig;
